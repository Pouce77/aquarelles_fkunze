{% extends 'admin/baseAdmin.html.twig' %}

{% block title %}Ajouter une aquarelle{% endblock %}

{% block body %}
<div class="m-3 w-50 p-3 border border-primary rounded shadow">
    {{ form_start(form) }}
    {{form_rest(form) }}
    
    {% if app.request.get('_route') == "app_painting_add" %}
    <input class='btn btn-primary' type="submit" value="Ajouter l'oeuvre"></input>
    {% else %}
         <input class='btn btn-primary' type="submit" value="Modifier l'oeuvre"></input>
    {% endif %}
</div>
{% endblock %}
