{% extends 'base.html.twig' %}

{% block body %}


    <div class="text-center m-5 fontTitle">
    <p>Entrez votre Email pour recevoir un mail de réinitialisation de votre mot de passe</p>
    </div>
    <div class="m-auto mt-5 w-50 p-3 border border-primary rounded shadow">
      {{ form_start(formReset) }}
        {{form_row(formReset.email)}}
        <input class="btn btn-primary" type='submit'></input>
      {{ form_end(formReset) }}
    
    </div>
{% endblock %}
